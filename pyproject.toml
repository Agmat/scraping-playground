[project]
name = "elec-scraping"
version = "0.1.0"
description = "GPI UMM (Electricity) scraper"
requires-python = ">=3.12"
dependencies = [
    "scrapling[fetchers]>=0.4",
]

[tool.uv]
package = false
